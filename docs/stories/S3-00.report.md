# S3-00 Builder Report

## Acceptance criteria

| # | Criterion | Status | Evidence |
|---|-----------|--------|----------|
| 1 | Cold-start durability against real deployment | **PASS** | `docs/evidence/S3-00/durability-increment.json`, `durability-get.json`, `durability-meta.json` |
| 2 | Unset store env vars → 5xx naming missing variable, not 200/zero | **PASS** | `src/telemetry/__tests__/store.test.ts` API route tests |
| 3 | In-memory and real implementations share one test suite | **PASS** | `src/telemetry/__tests__/store.test.ts` parametrized over `memory` and `redis` backends |

## Deploy configuration (read from repo + Vercel)

| Setting | Value |
|---------|-------|
| Framework | Next.js 16.3.1 (App Router, TypeScript) |
| Host | Vercel (`sairamprojects/agent-cost-calc`) |
| Build region | Washington, D.C. (iad1) |
| Production URL | https://agent-cost-calc.vercel.app |
| Store | Upstash for Redis via Vercel Marketplace (`agent-cost-calc-telemetry`, `primaryRegion=iad1`, free plan) |
| Store env vars | `KV_REST_API_URL`, `KV_REST_API_TOKEN` (also `KV_URL`, `REDIS_URL`, `KV_REST_API_READ_ONLY_TOKEN` injected by integration) |
| Prior store in repo | None — no existing KV/SQL vendor wired |

## Durability proof (AC1)

1. Deployed `dpl_AgTZ5QCiqN6raFavYFLoa6YCDkKT` to production.
2. `POST /api/telemetry/increment` with `bucket=s3-00-durability`, `key=cold-start-20260910T103200Z` → `value: 1` at `2026-09-10T10:32:01.672Z`.
3. Forced fresh deployment `dpl_EuRKnvgVtabRYbzAvfsnzc2UgNTq` (`vercel deploy --prod --force`).
4. `GET /api/telemetry/get` from new invocation → `value: 1` at `2026-09-10T10:32:44.739Z`.

Counter survived cold start / redeploy.

## Files changed

- `src/telemetry/store.ts` — `TelemetryStore` interface, memory + Upstash Redis implementations, env validation
- `src/telemetry/api.ts` — shared error handling for API routes
- `src/app/api/telemetry/increment/route.ts` — POST increment endpoint
- `src/app/api/telemetry/get/route.ts` — GET counter endpoint
- `src/telemetry/__tests__/store.test.ts` — shared suite for memory + redis backends, env-missing API tests
- `vitest.config.ts` — `@/` path alias for tests
- `package.json` / `package-lock.json` — `@upstash/redis` dependency
- `README.md` — Deployment section for telemetry store
- `docs/evidence/S3-00/tests.json` — test evidence (script-generated)
- `docs/evidence/S3-00/durability-*.json` — cold-start proof artifacts

## Assumptions

- Vercel KV is sunset; **Upstash for Redis** via Vercel Marketplace is the supported successor and uses the same `KV_*` env var names.
- HTTP probe endpoints (`/api/telemetry/increment`, `/api/telemetry/get`) satisfy AC2's "endpoint" requirement; full telemetry logic is deferred to S3-02.
- Redis backend tests run locally when `KV_REST_API_URL` and `KV_REST_API_TOKEN` are present (pulled via `vercel env pull`).

## Evidence hash

```
sha256=555cb015e82131bbe3d9c7b55c9d72b56db58af4031eeb7edd0e0d19ce9b80f1
```

`bash scripts/check-evidence.sh S3-00` → **PASS**

## Out of scope, noticed

- `/trace` page still exists as a retirement notice; no telemetry wiring there yet.
- `append` / `list` have no HTTP routes yet (store module only); S3-02 may add them.
